import { Store } from "@mui/icons-material";
import React from "react";
import { SvgIconProps } from "@mui/material/SvgIcon";

const BuyInIcon: React.FC<SvgIconProps> = (props) => (
  <Store id="buyin-icon" {...props} />
);

export default BuyInIcon;
